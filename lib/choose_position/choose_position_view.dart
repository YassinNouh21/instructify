import 'package:flutter/material.dart';

class ChoosePositionView extends StatelessWidget {
  const ChoosePositionView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ChoosePosition'),
      ),
    );
  }
}