import 'package:flutter/material.dart';

class RegisterTwoView extends StatelessWidget {
  const RegisterTwoView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('RegisterTwo'),
      ),
    );
  }
}
